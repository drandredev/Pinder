<?php

use Illuminate\Database\Seeder;

class PinderCollectionSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        #factory(App\Post::class, 9)->create();

        #$users = factory(App\User::class,4)
        #->create()
        #->each(function($user){
         #   $user->post()->createMany(
          #      factory(App\Post::class,2)->make()->toArray()
           # ); 
         #});

        //
    }
}
